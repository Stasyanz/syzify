{
  "id": "com.syzify.example.paste-import",
  "name": "Paste Import (example)",
  "version": "0.1.0",
  "minAppVersion": "0.6.0",
  "author": "Syzify",
  "description": "Reference WASM plugin: imports a pasted GPX/TCX into the vault through host_import_file — the smallest possible sync plugin.",
  "entry": "plugin.wasm",
  "contributes": ["dashboard.widget"],
  "permissions": ["import:files"]
}
